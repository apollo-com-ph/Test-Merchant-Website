name: Test Merchant Website
on: 
  push:
    branches: 
      - test
    paths-ignore:
      - 'k8s/manifests/**'

jobs: 
  build-image:
    runs-on: ubuntu-latest

    steps: 
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.AM1_DOCKER_REGISTRY }}
          username: ${{ secrets.AM1_DOCKER_USER }}
          password: ${{ secrets.AM1_DOCKER_PASS }}

      - name: Build and Push Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.AM1_DOCKER_REGISTRY }}/payment-gateway-v2/merchant-website:1.0.0-test